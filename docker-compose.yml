name: paceday-web

services:
  web:
    build:
      context: .
    image: paceday-web:dev
    ports: ["8080:8080"]
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:8080/"]
      interval: 15s
      timeout: 3s
      retries: 5
    restart: unless-stopped
