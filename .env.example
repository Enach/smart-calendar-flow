# Backend API base URL — used by src/lib/api.ts.
# Defaults to http://localhost:3001 when unset.
VITE_API_URL=http://localhost:3001

# Sentry frontend DSN (PAC-14). Client-side identifier, not a secret.
# Leave unset to fall back to the documented default DSN; set empty or override
# per environment. Unsetting disables monitoring (runtime kill switch).
VITE_SENTRY_DSN=https://8141ab6e48ded81c6d47f8f074c1a648@o4512081160896512.ingest.de.sentry.io/4512083551060048
